<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Dojo Survey</title>
    <link rel="stylesheet" type="text/css" href="style.css">
  </head>
  <body>
    <h2>Here are the awesome quotes</h2>
  <% for(var quote = all_quotes.length-1; quote >= 0; quote--) { %>
  <div>
    <h4>"<%= all_quotes[quote].quote %>"</h4>
    <p>-<%= all_quotes[quote].name %> at <%= all_quotes[quote].created_at %></p>
    <hr>
  </div>
  <% } %>
  </body>
</html>
